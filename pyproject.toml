[project]
name = "whatsapp-blast"
version = "0.1.0"
description = "Send a Twilio WhatsApp Content Template to every contact in a CSV, safely."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
whatsapp-blast = "whatsapp_blast.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
